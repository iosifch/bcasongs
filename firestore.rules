rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /songs/{song} {
      // Allow anyone to read songs
      allow read: if true;
      
      // Allow only authenticated users to write (add, update, delete)
      allow write: if request.auth != null;
    }
  }
}
